# MEMEFinder 构建指南

## 📦 文件结构说明

### 核心程序文件
- **main.py** - 程序入口，启动GUI
- **download_models.py** - AI模型下载工具
- **requirements.txt** - Python依赖列表
- **meme_finder.db** - SQLite数据库（自动生成）

### 源代码目录
- **src/** - 核心源代码
  - **core/** - 核心功能（数据库、OCR、扫描）
  - **gui/** - 图形界面（主窗口、标签页）
  - **utils/** - 工具模块（日志、资源监控）

### 构建脚本

#### Python构建脚本
- **build_exe.py** - 标准打包脚本（基础版）
- **build_optimized.py** - 优化打包脚本（推荐，包含大小优化和二进制过滤）

#### 批处理脚本（Windows用户推荐）
- **一键打包_无清理.bat** - 直接打包（不清理dist，避免文件锁）
- **强制清理.bat** - 强制清理dist/build目录（重启资源管理器）
- **创建安装程序.bat** - 使用Inno Setup创建Windows安装程序

### 安装程序配置
- **installer/setup.iss** - Inno Setup配置文件
- **installer/MEMEFinder-Setup-v*.exe** - 生成的安装程序（自动生成）

### 文档目录
- **docs/** - 项目文档
  - CHECKLIST.md - 检查清单
  - OPTIMIZATION_REPORT.md - 优化报告
  - PROJECT_SUMMARY.md - 项目摘要
  - QUICKSTART.md - 快速开始
  - STRUCTURE.md - 结构说明
  - TUTORIAL.md - 教程

---

## 🚀 构建流程

### 方案A：使用批处理脚本（推荐 Windows 用户）

#### 1. 标准打包流程
```batch
# 第一次打包或需要完全重建
.\强制清理.bat         # 清理旧文件
.\一键打包_无清理.bat   # 开始打包
```

#### 2. 快速重新打包（避免文件锁）
```batch
# 直接覆盖打包，不清理
.\一键打包_无清理.bat
```

#### 3. 创建安装程序
```batch
# 前提：已经运行过打包脚本，dist/MEMEFinder 存在
.\创建安装程序.bat
```

**输出位置：**
- 打包结果：`dist/MEMEFinder/`
- ZIP压缩包：`dist/MEMEFinder.zip`
- 安装程序：`installer/MEMEFinder-Setup-v1.0.0.exe`

---

### 方案B：使用Python脚本

#### 基础打包
```bash
python build_exe.py
```

#### 优化打包（推荐，包含大小优化）
```bash
python build_optimized.py
```

**优化特性：**
- ✅ 修复unittest缺失问题（PaddlePaddle需要）
- ✅ 排除40+不需要的模块（pytest, jupyter, matplotlib等）
- ✅ 二进制过滤（移除Qt/matplotlib/scipy的DLL）
- ✅ UPX压缩
- ✅ 自动计算文件大小

---

## 🔧 常见问题

### Q1: 打包时提示"PermissionError: 另一个程序正在使用此文件"
**原因：** Windows资源管理器或杀毒软件锁定了dist目录

**解决方案：**
```batch
# 方案1：强制清理（会重启资源管理器）
.\强制清理.bat

# 方案2：使用无清理模式打包
.\一键打包_无清理.bat

# 方案3：手动操作
1. 关闭所有资源管理器窗口
2. 任务管理器结束explorer.exe
3. 手动删除dist和build目录
4. 重新运行打包
```

### Q2: 运行打包程序时提示"ModuleNotFoundError: No module named 'unittest'"
**原因：** PyInstaller没有包含unittest模块（PaddlePaddle运行时需要）

**解决方案：** 已在所有构建脚本中修复，使用最新的构建脚本即可

### Q3: 打包后文件太大（>1GB）
**原因：** PaddlePaddle深度学习框架体积很大

**已实施的优化：**
- 排除matplotlib、scipy、pandas等大型库
- 排除pytest、jupyter等开发工具
- 二进制过滤（移除不必要的DLL）
- UPX压缩

**预期大小：**
- 原始：~1027 MB
- 压缩ZIP：~602 MB
- 优化后：预计减少10-20%

### Q4: Inno Setup找不到或无法编译
**检查清单：**
1. 确保已安装 Inno Setup 6（下载：https://jrsoftware.org/isdl.php）
2. 确保dist/MEMEFinder目录存在且包含MEMEFinder.exe
3. 检查installer/setup.iss文件路径是否正确

**手动编译：**
```
打开 "C:\Program Files (x86)\Inno Setup 6\Compil32.exe"
加载 installer\setup.iss
点击 "编译" 或按 F9
```

---

## 📋 构建检查清单

### 打包前
- [ ] 已安装所有依赖：`pip install -r requirements.txt`
- [ ] 代码无语法错误
- [ ] 已测试main.py能正常运行
- [ ] 已清理旧的dist/build目录（或使用无清理模式）

### 打包后
- [ ] 检查dist/MEMEFinder/MEMEFinder.exe存在
- [ ] 检查dist/MEMEFinder/_internal目录包含所有依赖
- [ ] 检查README.md已复制到dist/MEMEFinder/
- [ ] 测试运行MEMEFinder.exe（不依赖Python环境）
- [ ] 测试AI模型下载功能

### 创建安装程序后
- [ ] 检查installer/MEMEFinder-Setup-v1.0.0.exe存在
- [ ] 测试安装程序（在干净的Windows系统）
- [ ] 测试卸载功能
- [ ] 测试快捷方式

---

## 🎯 发布流程

### 1. 打包程序
```batch
.\强制清理.bat
.\一键打包_无清理.bat
```

### 2. 本地测试
- 运行 `dist\MEMEFinder\MEMEFinder.exe`
- 测试所有功能（扫描、OCR、搜索）
- 测试模型下载

### 3. 创建安装程序
```batch
.\创建安装程序.bat
```

### 4. 测试安装程序
- 在另一台电脑或虚拟机测试安装
- 测试程序功能
- 测试卸载

### 5. 上传到GitHub Release
```bash
# 创建Release
gh release create v1.0.0 \
  installer/MEMEFinder-Setup-v1.0.0.exe \
  dist/MEMEFinder.zip \
  --title "MEMEFinder v1.0.0" \
  --notes "首个稳定版本发布"
```

---

## 💡 技术细节

### PyInstaller配置
- **模式：** `--windowed`（无控制台窗口）
- **关键hiddenimports：** unittest, unittest.mock, doctest, paddleocr, paddle
- **排除模块：** matplotlib, scipy, pandas, jupyter, pytest等40+模块
- **数据文件：** src目录, README.md

### Inno Setup配置
- **压缩：** lzma2/max
- **架构：** x64compatible
- **安装后任务：** 自动下载AI模型（可选）
- **快捷方式：** 桌面 + 开始菜单

### 文件锁问题
Windows的资源管理器和杀毒软件会锁定正在查看的目录，导致无法删除。解决方案：
1. 使用 `--noconfirm` 而非 `--clean` 参数（覆盖而非删除）
2. 强制清理脚本会重启explorer.exe
3. 手动关闭所有资源管理器窗口

---

## 📞 支持

如有问题，请查看：
- 主README：[README.md](README.md)
- 项目文档：[docs/](docs/)
- GitHub Issues：[提交问题](https://github.com/aliveriver/MEMEFinder/issues)
